<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tacker.mycamera">

    <users-feature android:name="android.hardware.camera2"
                   android:required="true" />
    <users-feature android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <users-feature android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowImageActivity" />
        <activity android:name=".SaveImageActivity" />
        <activity android:name=".SimpVideoActivity" />
        <activity android:name=".SuperCameraActivity" />
    </application>

</manifest>